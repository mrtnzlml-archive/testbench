language: php

php:
  - 5.4
  - 5.5
  - 5.6
  - 7.0
  - hhvm

before_script:
  - composer selfupdate
  - composer install --prefer-dist --dev

script:
  - vendor/run-tests

notifications:
  email: false
